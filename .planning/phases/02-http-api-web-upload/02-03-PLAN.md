---
phase: 02-http-api-web-upload
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/middleware/apiKey.ts
  - src/middleware/session.ts
  - src/middleware/authOrApiKey.ts
  - src/server.ts
  - src/index.ts
  - src/routes/upload.ts
  - tests/e2e/upload.happy-path.test.ts
  - tests/e2e/upload.fallback.test.ts
  - tests/e2e/upload.digicert-success.test.ts
  - tests/e2e/auth.test.ts
autonomous: true
requirements:
  - UPLOAD-01
  - SEC-01
user_setup: []
must_haves:
  truths:
    - "POST /api/upload without X-API-Key returns 401 with {error:true,code:'UNAUTHORIZED',message:string} envelope (D-23/D-24)."
    - "POST /api/upload with the wrong X-API-Key returns 401 with same envelope, using timing-safe compare (D-01)."
    - "POST /api/upload with the correct X-API-Key still archives the file AND inserts a row into archive_entries; response includes the archive id (D-21)."
    - "Process exits before binding port 3000 if API_KEY, SESSION_SECRET, or ADMIN_PASSWORD is missing (D-06)."
    - "Phase 1's '⚠ Phase 1 has no auth' warning is removed; replaced with one-line auth-active startup log (D-26)."
    - "Existing Phase 1 e2e suites (upload.happy-path, upload.fallback, upload.digicert-success) still pass after harness updates."
  artifacts:
    - path: "src/middleware/apiKey.ts"
      provides: "apiKeyMiddleware(expected) -> MiddlewareHandler with timing-safe compare"
      exports: ["apiKeyMiddleware"]
    - path: "src/middleware/session.ts"
      provides: "requireSessionApi(deps), requireSessionPage(deps) middlewares"
      exports: ["requireSessionApi", "requireSessionPage"]
    - path: "src/middleware/authOrApiKey.ts"
      provides: "authOrApiKey(deps) accepting EITHER X-API-Key OR signed session cookie"
      exports: ["authOrApiKey"]
    - path: "tests/e2e/auth.test.ts"
      provides: "401 envelope coverage for missing/wrong key"
  key_links:
    - from: "src/server.ts"
      to: "src/middleware/apiKey.ts"
      via: "apiKeyMiddleware(deps.config.apiKey) on POST /api/upload"
      pattern: "apiKeyMiddleware\\(deps"
    - from: "src/index.ts"
      to: "src/lib/config.ts + src/db/client.ts + src/db/backfill.ts"
      via: "loadConfig -> openDb -> backfillManifest -> createApp(deps) -> serve(...)"
      pattern: "backfillManifest.*createApp"
    - from: "src/routes/upload.ts (modified)"
      to: "src/db/schema.ts (archiveEntries)"
      via: "deps.db.insert(archiveEntries).values(...).run() inside D-21 try/catch"
      pattern: "insert\\(archiveEntries\\)"
---

<objective>
Wave 2 wiring. Plug Plan 01 (config + error envelope) and Plan 02 (DB + backfill)
into the running application:

1. Build the three middlewares (apiKey, session, authOrApiKey) on top of Plan 01's
   helpers.
2. Convert `createApp()` -> `createApp(deps)`; rewrite `src/index.ts` to load
   config, open the DB, run the backfill, and pass a deps bag in.
3. Rewire `src/routes/upload.ts` to (a) use the apiKey middleware, (b) emit the
   D-23 error envelope on every non-2xx, (c) INSERT into archive_entries after
   writeBundle returns per the D-21 orphan-handling rule.
4. Update the Phase 1 e2e harness so existing tests still pass with the new
   env-var requirements + deps bag.
5. Ship a fresh `tests/e2e/auth.test.ts` covering the new 401 path.

Purpose: by end of this plan, SEC-01 + UPLOAD-01 are demonstrably true on a
running server, the database table is populated by real uploads, and the orphan
recovery path (DB-fail-after-bundle-write) is exercised.

Output: 3 middleware files (new), 2 modified bootstrap files, 1 modified route,
3 modified Phase 1 e2e tests, 1 new e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-http-api-web-upload/02-CONTEXT.md
@.planning/phases/02-http-api-web-upload/02-PATTERNS.md
@.planning/phases/02-01-PLAN.md
@.planning/phases/02-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Consumed: -->

From Plan 01:
- `src/lib/config.ts`: loadConfig(), AppConfig
- `src/middleware/errorEnvelope.ts`: errorResponse(c, status, code, message), registerErrorEnvelope(app), ErrorCode union
- `src/lib/sessionCookie.ts`: verifySessionCookie(cookie, secret), SessionPayload

From Plan 02:
- `src/db/client.ts`: openDb(path), Db type
- `src/db/schema.ts`: archiveEntries
- `src/db/backfill.ts`: backfillManifest({db, dataDir})

<!-- Produced (consumed by Plans 04 + 05): -->

The new deps bag shape:
```typescript
export interface AppDeps {
  db: Db;
  config: AppConfig;
}
export function createApp(deps: AppDeps): Hono;
```

`apiKeyMiddleware(expected: string): MiddlewareHandler`
`requireSessionApi(deps: AppDeps): MiddlewareHandler` — 401 envelope on bad/missing cookie
`requireSessionPage(deps: AppDeps): MiddlewareHandler` — 303 redirect to /login?next=… on bad/missing cookie
`authOrApiKey(deps: AppDeps): MiddlewareHandler` — accepts EITHER header (D-12)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three middlewares (apiKey + session + authOrApiKey)</name>
  <files>
    src/middleware/apiKey.ts,
    src/middleware/session.ts,
    src/middleware/authOrApiKey.ts,
    tests/e2e/auth.test.ts
  </files>
  <read_first>
    - .planning/phases/02-http-api-web-upload/02-CONTEXT.md (D-01, D-02, D-04, D-12, D-23, D-24)
    - .planning/phases/02-http-api-web-upload/02-PATTERNS.md lines 112-165 (middlewares)
    - src/lib/sourceIp.ts (closest analog: per-request helper reading c.req)
    - src/middleware/errorEnvelope.ts (Plan 01 — must be imported for 401 envelope)
    - src/lib/sessionCookie.ts (Plan 01 — verifySessionCookie())
    - tests/e2e/upload.happy-path.test.ts lines 1-50 (e2e harness pattern to clone)
  </read_first>
  <behavior>
    apiKeyMiddleware (e2e via auth.test.ts):
    - POST /api/upload without X-API-Key -> 401, JSON body matches {error:true, code:"UNAUTHORIZED", message:string}, message is German.
    - POST /api/upload with X-API-Key: "wrong" -> 401, same envelope.
    - POST /api/upload with X-API-Key: process.env.API_KEY (correct) and a valid file -> 201 with archive id (verified separately in upload.happy-path).
    - X-API-Key check uses Node.js crypto.timingSafeEqual with length pre-check (asserted via grep on the file in acceptance_criteria).

    requireSessionApi (e2e via auth.test.ts, exercised on a small mounted test endpoint OR re-used via /api/download once Plan 05 lands — for this plan, mount a temporary `app.get("/api/_session-probe", requireSessionApi(deps), c => c.json({ok:true}))` ONLY inside the test file or assert behavior via a synthetic Hono harness):
    - No cookie -> 401 UNAUTHORIZED envelope.
    - Cookie with wrong secret -> 401.
    - Cookie tampered (flip char in body) -> 401.
    - Cookie expired -> 401.
    - Valid cookie -> next() runs (probe returns 200 ok).

    requireSessionPage:
    - No cookie on GET /some-page -> 303 redirect with Location: /login?next=%2Fsome-page (url-encoded next).
    - Invalid cookie -> 303 redirect to /login?next=...
    - Valid cookie -> next() runs.

    authOrApiKey:
    - Valid X-API-Key, no cookie -> next() runs.
    - Valid session cookie, no X-API-Key -> next() runs.
    - Neither -> 401 envelope.
    - Wrong X-API-Key AND valid cookie -> next() runs (cookie fallback wins; absence of API key is not failure, only wrong + no cookie is failure).
  </behavior>
  <action>
    Create src/middleware/apiKey.ts per PATTERNS.md lines 130-145. Signature: `export function apiKeyMiddleware(expected: string): MiddlewareHandler`. Pre-encode `expectedBuf = Buffer.from(expected, "utf8")` outside the returned closure. Inside: read `c.req.header("x-api-key") ?? ""`, build `providedBuf`, branch on length mismatch first (timingSafeEqual throws on unequal lengths) → return errorResponse(c, 401, "UNAUTHORIZED", "Nicht authentifiziert."). Otherwise compare timing-safe; on mismatch, same 401 envelope. On match, `await next()`.

    Create src/middleware/session.ts. Two factories:
    - `requireSessionApi(deps)`: read `c.req.header("cookie")` → parse `session=...` value (manual split is fine; Hono has `getCookie` but the codebase has no helper yet — use Hono's getCookie from "hono/cookie" for safety) → call `verifySessionCookie(value, deps.config.sessionSecret)` → null branch returns errorResponse(c, 401, "UNAUTHORIZED", "Nicht authentifiziert."). On success, `c.set("session", payload)` then `await next()`.
    - `requireSessionPage(deps)`: same lookup; null branch returns `c.redirect("/login?next=" + encodeURIComponent(c.req.path), 303)`. Use 303 (POST-then-GET pattern in login redirect chain).

    Create src/middleware/authOrApiKey.ts. Composition: check X-API-Key with timing-safe compare against `deps.config.apiKey`; on success, next(). On fail/absent, try the session-cookie path (same logic as requireSessionApi). On both-failed, return errorResponse(c, 401, "UNAUTHORIZED", "Nicht authentifiziert."). Per D-12 wording: "accepts either".

    Create tests/e2e/auth.test.ts cloning the upload.happy-path.test.ts harness:
    - beforeAll sets API_KEY, SESSION_SECRET (≥32 bytes), ADMIN_PASSWORD, DATA_DIR (tmp), MANIFEST_DB_PATH (tmp).
    - Build deps inline: `const config = loadConfig(); const db = openDb(config.manifestDbPath); const app = createApp({db, config});` — note: createApp now takes deps (Task 2 below adjusts the factory).
    - Test cases: GET /health still 200 (sanity); POST /api/upload with no X-API-Key → 401 + envelope assertion; POST /api/upload with wrong X-API-Key → 401 + envelope; (positive happy-path assertion stays in upload.happy-path.test.ts after harness update in Task 3).
    - For the session probe assertion (5 cases listed in &lt;behavior&gt; for requireSessionApi), mount the probe route ONLY inside the test, by calling app.get(...) after createApp returns — `app.get("/_probe", requireSessionApi({...deps}), c => c.json({ok:true}))` — and exercise with crafted Cookie headers built via signSessionCookie(payload, secret) for the success path.

    ESM: every sibling import uses `.js`. node:crypto, node:path, etc. always with `node:` prefix.
  </action>
  <verify>
    <automated>pnpm test -- tests/e2e/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/e2e/auth.test.ts` exits 0.
    - `grep -c "timingSafeEqual" src/middleware/apiKey.ts` ≥ 1
    - `grep -cE "expectedBuf\\.length !== |providedBuf\\.length !== " src/middleware/apiKey.ts` ≥ 1 (length pre-check before timingSafeEqual)
    - `grep -c "errorResponse(c, 401, \"UNAUTHORIZED\"" src/middleware/apiKey.ts src/middleware/session.ts src/middleware/authOrApiKey.ts | grep -v ':0$'` returns ≥3 (each middleware emits the standardized 401).
    - `grep -c "c.redirect" src/middleware/session.ts` ≥ 1 (page variant redirects to /login)
    - `grep -c "encodeURIComponent" src/middleware/session.ts` ≥ 1 (next= param is encoded)
    - `grep -nE "import.*from \"[^\"]+\\.(ts|tsx)\"" src/middleware/` returns no matches.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Three middlewares export the agreed signatures; auth.test.ts asserts the unauth + happy paths; timing-safe compare and German error envelope are both verified.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap rewire — createApp(deps) + index.ts orchestration</name>
  <files>
    src/server.ts,
    src/index.ts
  </files>
  <read_first>
    - .planning/phases/02-http-api-web-upload/02-CONTEXT.md (D-06, D-20, D-21, D-26)
    - .planning/phases/02-http-api-web-upload/02-PATTERNS.md lines 49-110 (server.ts + index.ts changes)
    - src/server.ts (current) — entire file
    - src/index.ts (current) — entire file
    - src/lib/config.ts (Plan 01)
    - src/db/client.ts + src/db/backfill.ts (Plan 02)
    - src/middleware/errorEnvelope.ts (Plan 01 — registerErrorEnvelope)
    - src/middleware/apiKey.ts (Task 1 above)
  </read_first>
  <action>
    Rewrite src/server.ts to:
    - import `registerErrorEnvelope` from middleware/errorEnvelope.js, `apiKeyMiddleware` from middleware/apiKey.js, `registerUpload` from routes/upload.js, `Hono` from hono, AppConfig from lib/config.js, Db from db/client.js.
    - Export `interface AppDeps { db: Db; config: AppConfig; }`.
    - Replace `createApp(): Hono` with `createApp(deps: AppDeps): Hono`.
    - Inside: drop the `console.warn("⚠ Phase 1...")` block (D-26). Add a single `console.info("auto-archive ready (auth active)")` after registration completes.
    - Order of registration per PATTERNS.md guidance:
      1. `registerErrorEnvelope(app)` — must come first so app.onError / app.notFound take effect across all later handlers.
      2. `app.get("/health", c => c.json({ok:true}))` — unchanged.
      3. `registerUpload(app, deps)` — Task 3 modifies this signature.
    - NOTE: pages/static/login/download registrars are added by Plans 04 + 05; this plan ONLY wires the upload + health surface plus the global error envelope. Document the placeholder for the additional registrars in a comment so Plans 04+05 can locate the insertion point cleanly.

    Rewrite src/index.ts to follow PATTERNS.md lines 86-110:
    1. `import { loadConfig } from "./lib/config.js"`
    2. `import { openDb } from "./db/client.js"`
    3. `import { backfillManifest } from "./db/backfill.js"`
    4. Top-level async IIFE OR top-level await (project is ESM `"type":"module"` — top-level await works):
       ```
       let config;
       try { config = loadConfig(); } catch (err) { console.error("[startup]", (err as Error).message); process.exit(1); }
       const db = openDb(config.manifestDbPath);
       await backfillManifest({ db, dataDir: config.dataDir });   // synchronous-ish; must complete before serve()
       const app = createApp({ db, config });
       const port = Number(process.env.PORT ?? 3000);
       serve({ fetch: app.fetch, port }, info => console.log(`auto-archive listening on http://0.0.0.0:${info.port}`));
       ```
    5. The fail-fast log line must include WHICH env var is missing (Error message from loadConfig already carries that; just echo it).
    6. Do NOT use `dangerouslyDisableSandbox` or any test-mode shim — the standard env-var path is the only path.

    Sibling imports MUST end in `.js`. node:* always prefixed.
  </action>
  <verify>
    <automated>API_KEY=t SESSION_SECRET=this-is-a-test-session-secret-32+chars ADMIN_PASSWORD=p MANIFEST_DB_PATH=/tmp/aa-boot-test.sqlite DATA_DIR=/tmp/aa-data-test PORT=0 timeout 5 node --import tsx src/index.ts 2>&1 | grep -E "auto-archive (listening|ready)" ; rm -f /tmp/aa-boot-test.sqlite ; rm -rf /tmp/aa-data-test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 1 has no auth" src/server.ts` == 0 (D-26: warning removed).
    - `grep -c "auth active\|auto-archive ready" src/server.ts` ≥ 1
    - `grep -c "registerErrorEnvelope" src/server.ts` ≥ 1
    - `grep -c "createApp(deps: AppDeps)" src/server.ts` ≥ 1
    - `grep -c "loadConfig\|openDb\|backfillManifest" src/index.ts | grep -v '^0$'` indicates all three are called.
    - Boot smoke (verify block) prints either "auto-archive ready" or "auto-archive listening" — process actually binds the port.
    - `API_KEY="" SESSION_SECRET=long-enough-secret-for-32-bytes-yo ADMIN_PASSWORD=p timeout 3 node --import tsx src/index.ts` exits non-zero AND stderr contains "API_KEY" (D-06 fail-fast).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Bootstrap loads config, opens DB, runs backfill, then binds port. Missing env vars cause hard fail with a clear message. Phase 1 warning removed; auth-active info log present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upload route rewire — apiKey gate + envelope migration + DB insert (D-21)</name>
  <files>
    src/routes/upload.ts,
    tests/e2e/upload.happy-path.test.ts,
    tests/e2e/upload.fallback.test.ts,
    tests/e2e/upload.digicert-success.test.ts
  </files>
  <read_first>
    - .planning/phases/02-http-api-web-upload/02-CONTEXT.md (D-01, D-21, D-22, D-23, D-25)
    - .planning/phases/02-http-api-web-upload/02-PATTERNS.md lines 220-256 (upload.ts modification map)
    - src/routes/upload.ts (current) — lines 1-240
    - src/db/schema.ts (Plan 02 — archiveEntries table)
    - src/middleware/apiKey.ts (Task 1)
    - src/middleware/errorEnvelope.ts (Plan 01)
    - src/lib/config.ts (Plan 01 — maxUploadBytes comes from here, not the hard-coded MAX_BODY_BYTES)
    - tests/e2e/upload.happy-path.test.ts (current) — lines 1-end
    - tests/e2e/upload.fallback.test.ts (current) — lines 1-end
    - tests/e2e/upload.digicert-success.test.ts (current) — lines 1-end
  </read_first>
  <behavior>
    upload.ts modifications:
    - GIVEN POST /api/upload registered with `apiKeyMiddleware(deps.config.apiKey)` upstream, the route handler only runs after auth passes.
    - GIVEN a valid multipart upload and a successful TSA roundtrip, after `writeBundle()` returns the route INSERTs a row into `archive_entries`. On INSERT failure, the route emits errorResponse(c, 500, "INTERNAL_ERROR", "Unbekannter Fehler.") AND leaves the bundle on disk (D-21 explicit: do NOT delete; backfill will recover). The orphan is logged via console.error including the archive id and a "bundle stays on disk" marker.
    - Response on success is `{id, bundle_path}` with 201 (unchanged from Phase 1).
    - Old `c.json({error: ...}, status)` shapes are gone. Every non-2xx is routed through `errorResponse(...)` with the right code:
      - 401 (handled by middleware): UNAUTHORIZED
      - 400 invalid multipart / missing file part: INVALID_REQUEST + German message "Ungültige Anfrage."
      - 400 invalid label: INVALID_REQUEST + German message
      - 413 size cap: FILE_TOO_LARGE + "Datei zu groß. Maximale Größe: 100 MB." (D-23 + UI-SPEC error copy)
      - 502 AllTsasFailed: TSA_UNAVAILABLE + "Zeitstempel-Dienst nicht erreichbar. Bitte in einigen Minuten erneut versuchen." (PATTERNS.md divergence note — `chain` detail is logged via console.error, NOT returned in body).
      - 500 INTERNAL_ERROR for any other thrown error from the bundle write path.
    - `MAX_BODY_BYTES` constant is replaced with `deps.config.maxUploadBytes` (D-25). The `registerUpload(app, deps)` signature change requires plumbing deps through.

    e2e harness updates (each existing test file):
    - In beforeAll, set the three new required env vars (API_KEY, SESSION_SECRET, ADMIN_PASSWORD) and MANIFEST_DB_PATH (tmp) BEFORE constructing the app — otherwise loadConfig will throw and the test crashes.
    - Build `const config = loadConfig(); const db = openDb(config.manifestDbPath); const app = createApp({db, config});`
    - Every existing fetch() against /api/upload MUST now send `headers: { "X-API-Key": process.env.API_KEY }` — otherwise the request 401s and the existing assertions fail.
    - upload.happy-path.test.ts gains a new assertion: after the 201 response, query the DB (`db.select().from(archiveEntries).where(eq(archiveEntries.id, body.id)).get()`) and assert the row exists with bundle_dir matching the on-disk path and tsa_provider matching the response/metadata.json. This proves D-21 INSERT happens.
    - upload.fallback.test.ts: response body assertion changes from `{error:"all_tsas_failed", chain:[...]}` to `{error:true, code:"TSA_UNAVAILABLE", message:"Zeitstempel-Dienst nicht erreichbar..."}`. The previous `chain` field is now logged-only, not exposed in the response.
    - upload.digicert-success.test.ts: same envelope semantics on any error paths it covers; happy-path assertions just need the X-API-Key header.
  </behavior>
  <action>
    Modify src/routes/upload.ts:
    1. Change signature to `export function registerUpload(app: Hono, deps: AppDeps): void`. Import AppDeps from "../server.js" (or move AppDeps to a shared types file if cleaner; planner's discretion documented in SUMMARY).
    2. Register the route with the middleware: `app.post("/api/upload", apiKeyMiddleware(deps.config.apiKey), async (c) => { ... })`.
    3. Remove the hard-coded `MAX_BODY_BYTES`; replace with `const MAX_BODY_BYTES = deps.config.maxUploadBytes;` inside the registrar closure, passing it into `streamMultipart(req, MAX_BODY_BYTES)` (refactor streamMultipart to take the limit as a parameter).
    4. After `await writeBundle(...)` returns and BEFORE the success c.json:
       ```
       try {
         deps.db.insert(archiveEntries).values({
           id, original_filename: parsed.filename, mime_type: metadata.mime_type,
           size_bytes: parsed.sizeBytes, sha256: sha256Hex, created_at: createdAt,
           label: parsed.label, source_ip: sourceIp, tsa_provider: tsa.provider,
           tsa_status: "verified", tsa_attested_at: tsa.attestedAt,
           tsa_fallback_chain: JSON.stringify(tsa.fallbackChain),
           bundle_dir: bundlePath,
         }).run();
       } catch (dbErr) {
         console.error("[upload] DB insert failed for", id, "bundle stays on disk", dbErr);
         return errorResponse(c, 500, "INTERNAL_ERROR", "Unbekannter Fehler.");
       }
       ```
       The bundle is INTENTIONALLY left on disk per D-21 — backfill will index it on next boot.
    5. Replace every `c.json({error: ...}, status)` site with `errorResponse(c, status, code, message)` using the code table above. The `AllTsasFailed` branch must NOT include the chain array in the response body — log it via `console.error("[upload] all TSAs failed", err.chain);` then return the standardized 502 envelope.
    6. Confirm `class UploadError` stays internal (still used to thread 400/413 status from streamMultipart). At the route-level catch, translate `UploadError.status === 413` → `errorResponse(c, 413, "FILE_TOO_LARGE", "Datei zu groß. Maximale Größe: 100 MB.")`, `UploadError.status === 400` → `errorResponse(c, 400, "INVALID_REQUEST", "Ungültige Anfrage.")`.

    Modify the three e2e tests per &lt;behavior&gt; section above. Each beforeAll sets all four env vars (API_KEY, SESSION_SECRET, ADMIN_PASSWORD, MANIFEST_DB_PATH) before any import that calls loadConfig. Add explicit DB-row assertion in upload.happy-path.test.ts (this is the only e2e proof of D-21 that this plan ships; download.test.ts in Plan 05 will further exercise it).
  </action>
  <verify>
    <automated>pnpm test -- tests/e2e/upload.happy-path.test.ts tests/e2e/upload.fallback.test.ts tests/e2e/upload.digicert-success.test.ts tests/e2e/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four e2e tests in &lt;verify&gt; exit 0.
    - `grep -c "apiKeyMiddleware(deps.config.apiKey)" src/routes/upload.ts` ≥ 1
    - `grep -c "deps.db.insert(archiveEntries)" src/routes/upload.ts` ≥ 1
    - `grep -c "errorResponse(c," src/routes/upload.ts` ≥ 4 (one per: 400 invalid request, 400 invalid label, 413 file too large, 502 tsa unavailable, 500 internal — at least 4 distinct call sites).
    - `grep -c "all_tsas_failed" src/routes/upload.ts` == 0 (Phase 1 ad-hoc shape removed from response).
    - `grep -c "bundle stays on disk" src/routes/upload.ts` ≥ 1 (orphan log invariant per D-21).
    - `grep -c "deps.config.maxUploadBytes" src/routes/upload.ts` ≥ 1 (no hard-coded 100MB constant).
    - `grep -c "X-API-Key" tests/e2e/upload.happy-path.test.ts tests/e2e/upload.fallback.test.ts tests/e2e/upload.digicert-success.test.ts` ≥ 3 (every test sends the header).
    - `grep -c "archiveEntries\|archive_entries" tests/e2e/upload.happy-path.test.ts` ≥ 1 (DB-row assertion exists).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Upload route is API-key-gated, every non-2xx flows through the D-23 envelope, every successful upload inserts a row, the DB-fail-after-bundle-write path is logged + envelope-returned without deleting the bundle, and the existing Phase 1 test suite still passes (now with auth headers).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external HTTP ↔ /api/upload | first auth surface; untrusted multipart + headers |
| process startup ↔ env | hard fail before any port bind on missing secrets |
| TSA chain failure ↔ response body | error chain detail must not leak provider URLs/keys |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | apiKeyMiddleware | mitigate | crypto.timingSafeEqual + length pre-check; D-01 |
| T-02-10 | Tampering | upload.ts DB write | mitigate | D-21 order: bundle on disk → INSERT → respond. On INSERT failure, log + 500 envelope; backfill recovers on next boot. No partial-state rollback to delete the on-disk bundle (it carries the cryptographic proof). |
| T-02-11 | Information Disclosure | TSA failure response | mitigate | `AllTsasFailed.chain` array logged server-side only; response body is generic German message (no provider URLs); D-23 |
| T-02-12 | Information Disclosure | apiKey 401 | mitigate | German message identical for "missing" and "wrong" — no oracle (PATTERNS.md error envelope). |
| T-02-13 | Denial of Service | streamMultipart fileSize | mitigate | busboy limits.fileSize → busboy emits "limit" → fail() → 413 envelope; D-22. Body never buffered in memory; streamed to tmp file. |
| T-02-14 | Elevation of Privilege | createApp(deps) | mitigate | deps bag is injected at startup from loadConfig (env-only) — routes cannot reach process.env directly for credentials. |
| T-02-15 | Spoofing | requireSessionApi cookie compare | mitigate | HMAC verified via timingSafeEqual inside sessionCookie.ts (Plan 01); D-02 |
| T-02-16 | Repudiation | startup env-var fail | mitigate | console.error includes the missing var name; D-06 |
</threat_model>

<verification>
- `pnpm test` exits 0 across all suites (Phase 1 + Plan 01 + Plan 02 + this plan).
- `npx tsc --noEmit` exits 0.
- Phase 1 warning string "⚠ Phase 1 has no auth" no longer appears in src/.
- Boot path: loadConfig → openDb → backfillManifest → createApp(deps) → serve. None can be skipped or reordered without breaking the smoke command in Task 2 verify block.
</verification>

<success_criteria>
- POST /api/upload: 401 without correct X-API-Key, 201 with correct, with the response carrying an archive id that maps to a real archive_entries row.
- Process refuses to boot if API_KEY/SESSION_SECRET/ADMIN_PASSWORD are missing.
- All /api/* errors emit the D-23 envelope; no Phase 1 ad-hoc shapes remain.
- D-21 invariant covered: DB insert-fail path is logged + 500 envelope, bundle stays on disk for backfill recovery.
</success_criteria>

<output>
After completion, create `.planning/phases/02-http-api-web-upload/02-03-SUMMARY.md` with: middleware signatures shipped, AppDeps shape location (server.ts vs. shared types file), the error-code-to-status mapping table that landed in upload.ts, a note on the e2e harness change pattern, and confirmation that Phase 1 e2e suites pass post-migration.
</output>
