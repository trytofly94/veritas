---
phase: 01-core-archive-engine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/tsa.ts
  - src/lib/tsaProviders.ts
  - src/lib/verifyTsr.ts
  - src/lib/bundle.ts
  - src/routes/upload.ts
  - src/lib/metadata.ts
  - assets/verify-template.sh
  - assets/tsa-certs/freetsa.pem
  - assets/tsa-certs/digicert.pem
  - assets/tsa-certs/README.md
  - tests/e2e/upload.fallback.test.ts
  - tests/e2e/upload.digicert-success.test.ts
  - tests/e2e/verify-script.test.ts
  - tests/unit/tsa.fallback.test.ts
autonomous: true
requirements: [CORE-02, CORE-03, CORE-04, SEC-03]
tags: [rfc3161, openssl, fallback, verify, immutability]
must_haves:
  truths:
    - "When DFN-TSA is unreachable the system retries FreeTSA and records the fallback in metadata.json"
    - "When both DFN and FreeTSA fail the system retries DigiCert AND succeeds against the real DigiCert endpoint (proven by a dedicated e2e test, CONCERN-6)"
    - "When all three TSAs fail the server returns 5xx and writes nothing to disk (D-05 invariant holds)"
    - "The TSA timeout path is exercised by a test that points at a sloth endpoint (setTimeout > 10s), not only at a closed port"
    - "Every successful bundle contains a verify.sh script that recomputes SHA-256 via sha256sum -c AND runs openssl ts -verify, exiting 0 on success with a clear pass message"
    - "Pre-finalization: the TSR is verified against the CA cert chain before the bundle is written (no unverified TSR ever reaches disk)"
    - "tsa-cacert.pem in each bundle matches the provider recorded in metadata.json.tsa_provider"
    - "FreeTSA and DigiCert CA chains committed under assets/tsa-certs/ are real cert chains (root + intermediates discovered via live ts -query), proven via openssl ts -verify exit 0 before commit (BLOCKER-1)"
  artifacts:
    - path: "src/lib/tsaProviders.ts"
      provides: "TSA_PROVIDERS array in priority order [{id:'dfn',endpoint,caCertPath}, freetsa, digicert] (D-12 order)"
    - path: "src/lib/tsa.ts"
      provides: "requestTimestampWithFallback(sha256Hex) iterating providers, returning {tsr, tsq, provider, attestedAt, fallbackChain} or throwing AllTsasFailed"
    - path: "src/lib/verifyTsr.ts"
      provides: "verifyTsr({tsr, dataPath, caCertPath}) invoking openssl ts -verify and throwing on failure"
    - path: "assets/verify-template.sh"
      provides: "verify.sh template copied verbatim into every bundle (POSIX sh, no bashisms beyond what bookworm-slim ships)"
    - path: "assets/tsa-certs/freetsa.pem"
      provides: "FreeTSA CA chain (root + intermediates discovered from live TSR), verified via openssl ts -verify exit 0 before commit"
    - path: "assets/tsa-certs/digicert.pem"
      provides: "DigiCert TSA CA chain (root + intermediates discovered from live TSR), verified via openssl ts -verify exit 0 before commit"
  key_links:
    - from: "src/routes/upload.ts"
      to: "src/lib/tsa.ts requestTimestampWithFallback"
      via: "single call that returns whichever TSA succeeded with the full chain attempted"
      pattern: "requestTimestampWithFallback"
    - from: "src/lib/tsa.ts"
      to: "src/lib/verifyTsr.ts"
      via: "verify each candidate TSR immediately before accepting it; on verify failure treat the provider as failed and continue to next"
      pattern: "verifyTsr"
    - from: "src/lib/bundle.ts"
      to: "assets/verify-template.sh"
      via: "fs.copyFile(verify-template.sh, <bundle>/verify.sh) followed by chmod 555"
      pattern: "verify-template"
    - from: "<bundle>/verify.sh"
      to: "bundle contents (original.*, original.sha256, original.tsr, tsa-cacert.pem)"
      via: "sha256sum -c original.sha256 + openssl ts -verify -in original.tsr -data original.<ext> -CAfile tsa-cacert.pem"
      pattern: "openssl ts -verify"
---

<objective>
Wave 2 of Phase 1. Extend the Walking Skeleton from Plan 01-01 with the production-grade behaviors required by the phase success criteria: the full DFN→FreeTSA→DigiCert fallback chain with pre-finalization TSR verification, the `verify.sh` script in every bundle that performs the full chain check, and the hard-fail invariant when all three TSAs fail. After this plan, the bundle directory on disk is complete (7 artifacts), self-verifying, and the fallback semantics of SEC-03 + CORE-04 are observable end-to-end. The DigiCert success path is independently proven (CONCERN-6).

Purpose: Honor D-05 (no partial bundles, write nothing when chain exhausted), D-09 (verify.sh runs the full hash + signature chain), D-10 (CA cert matches provider), D-12 (`tsa_fallback_chain` populated).

Output: A user submitting a file gets a bundle whose `verify.sh` they can run weeks later to prove integrity, regardless of which TSA actually signed it — and if all TSAs are down, the user gets a clean error and zero disk pollution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-archive-engine/01-CONTEXT.md
@.planning/phases/01-core-archive-engine/01-SKELETON.md
@.planning/phases/01-core-archive-engine/01-01-SUMMARY.md
@src/lib/tsa.ts
@src/lib/bundle.ts
@src/routes/upload.ts
@src/lib/metadata.ts
@src/types.ts

<interfaces>
<!-- Contracts established by Plan 01-01 that this plan extends -->

From src/types.ts:
- type TsaProviderId = "dfn" | "freetsa" | "digicert"
- interface TsaResult { provider: TsaProviderId; tsq: Buffer; tsr: Buffer; attestedAt: string }
- interface Metadata { id; original_filename; mime_type; size_bytes; sha256; created_at; label; source_ip; tsa_provider; tsa_status; tsa_attested_at; tsa_fallback_chain }

From src/lib/tsa.ts (Plan 01-01):
- requestTimestamp(sha256Hex: string, provider: "dfn"): Promise<TsaResult> — NOTE: invocations do NOT pass `-no_nonce` (CONCERN-2); attestedAt parsed via pkijs (CONCERN-1)
  (this plan generalizes to accept any provider id and adds requestTimestampWithFallback)

From src/lib/bundle.ts (Plan 01-01):
- writeBundle(args): Promise<string> — already writes original.sha256 in sha256sum -c format `<hex>  original.<ext>\n` (BLOCKER-3 resolved upstream)
- Extend this plan to also copy verify-template.sh as verify.sh with mode 0o555 (executable + read-only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TSA fallback chain with pre-finalization verify + DigiCert success proof</name>
  <files>src/lib/tsaProviders.ts, src/lib/tsa.ts, src/lib/verifyTsr.ts, src/lib/metadata.ts, src/routes/upload.ts, assets/tsa-certs/freetsa.pem, assets/tsa-certs/digicert.pem, assets/tsa-certs/README.md, tests/unit/tsa.fallback.test.ts, tests/e2e/upload.fallback.test.ts, tests/e2e/upload.digicert-success.test.ts</files>
  <read_first>
    - src/lib/tsa.ts (current single-provider implementation from Plan 01-01; note nonce-default + pkijs-parsing established there)
    - src/routes/upload.ts (current handler wiring)
    - src/lib/metadata.ts (current buildMetadata signature)
    - .planning/phases/01-core-archive-engine/01-CONTEXT.md (D-05 hard-fail invariant, D-10 CA cert matching, D-12 metadata schema, D-14 source ip)
    - .planning/research/PITFALLS.md (TSA quirks — FreeTSA nonce defaults, DigiCert endpoint behavior)
    - .planning/research/ARCHITECTURE.md (TSA fallback order)
    - .planning/phases/01-core-archive-engine/01-01-SUMMARY.md (observed DFN latency, DFN cert chain composition, and any quirks)
    - assets/tsa-certs/README.md (cert-chain discovery procedure established in Plan 01-01)
    - https://www.freetsa.org/index_en.php (FreeTSA endpoint + nonce policy — do NOT pass `-no_nonce`)
    - https://docs.openssl.org/3.2/man1/openssl-ts/ (-verify flags)
  </read_first>
  <behavior>
    - Unit test tsa.fallback.test.ts: when the dfn fetch rejects, requestTimestampWithFallback calls freetsa; if that rejects too, calls digicert; returns the first success with fallback_chain reflecting providers attempted
    - Unit test: when all three providers reject, the function throws an AllTsasFailed error whose .chain is ["dfn","freetsa","digicert"]
    - Unit test: when a provider returns a TSR that fails openssl ts -verify, that provider is considered failed and the chain continues
    - Unit test (CONCERN nitpick): the AbortController timeout path is exercised by a mock endpoint that calls setTimeout(15_000) before responding — the test installs vitest fake timers, advances by >10s, and asserts the provider is recorded as failed via a timeout error (not just ECONNREFUSED)
    - E2E test upload.fallback.test.ts: with DFN endpoint blocked (mock via env override TSA_DFN_ENDPOINT pointing to a closed port), POST /api/upload completes 201 and produces a bundle whose metadata.json has tsa_provider==="freetsa" and tsa_fallback_chain deep-equals ["dfn","freetsa"] and tsa-cacert.pem byte-equals assets/tsa-certs/freetsa.pem
    - E2E test (CONCERN-6) upload.digicert-success.test.ts: with TSA_DFN_ENDPOINT and TSA_FREETSA_ENDPOINT both pointed at `http://127.0.0.1:1` (closed) but DigiCert allowed to reach the real http://timestamp.digicert.com, POST /api/upload returns 201 and the produced bundle has metadata.tsa_provider === "digicert", metadata.tsa_fallback_chain deep-equals ["dfn", "freetsa", "digicert"], tsa-cacert.pem byte-equals assets/tsa-certs/digicert.pem, AND `openssl ts -verify -in <bundle>/original.tsr -data <bundle>/original.<ext> -CAfile <bundle>/tsa-cacert.pem` exits 0. This test is gated on network reachability to DigiCert and skipped (with a clear `test.skipIf`) only when DigiCert is unreachable from CI; on the developer machine it MUST run.
    - E2E test: with DFN AND FreeTSA AND DigiCert all blocked via env overrides, the response is 502 with JSON body {error: "all_tsas_failed", chain: ["dfn","freetsa","digicert"]} AND no new directory appears in DATA_DIR (verified by snapshotting DATA_DIR entries before and after)
  </behavior>
  <action>Create src/lib/tsaProviders.ts exporting TSA_PROVIDERS = [{id:'dfn', endpoint: process.env.TSA_DFN_ENDPOINT ?? 'https://zeitstempel.dfn.de', caCertPath: 'assets/tsa-certs/dfn.pem', timeoutMs: Number(process.env.TSA_TIMEOUT_MS ?? 10000)}, {id:'freetsa', endpoint: process.env.TSA_FREETSA_ENDPOINT ?? 'https://freetsa.org/tsr', caCertPath: 'assets/tsa-certs/freetsa.pem', timeoutMs: ...}, {id:'digicert', endpoint: process.env.TSA_DIGICERT_ENDPOINT ?? 'http://timestamp.digicert.com', caCertPath: 'assets/tsa-certs/digicert.pem', timeoutMs: ...}] preserving D-12 order. The provider record may optionally carry a `noNonce: boolean` flag (default false) so that if any TSA later turns out to require nonceless requests it can be set per-provider — for v1, all three default to `noNonce: false` per CONCERN-2 / FreeTSA recommendation. **Commit assets/tsa-certs/freetsa.pem and assets/tsa-certs/digicert.pem (BLOCKER-1) using the live-discovery procedure documented in Plan 01-01's assets/tsa-certs/README.md** — for each provider: (i) build a real ts-query with a known digest (no -no_nonce), (ii) POST it to the live endpoint, (iii) inspect the returned TSR via `openssl ts -reply -in reply.tsr -text` and identify every cert in the signing chain, (iv) fetch root + intermediates from each authority's published trust store, (v) concatenate root + intermediates into the .pem file, (vi) prove `openssl ts -verify -in reply.tsr -queryfile query.tsq -CAfile assets/tsa-certs/<provider>.pem` exits 0 BEFORE committing, (vii) append the SHA-256 fingerprint + subject + issuer of every cert in each chain to assets/tsa-certs/README.md so cert changes are auditable. If any TSA endpoint is unreachable during planning execution, surface a CHECKPOINT rather than silently committing a guessed chain. Refactor src/lib/tsa.ts: split the single-provider HTTP+parse logic into requestTimestampFromProvider(sha256Hex, provider) returning TsaResult with the provider's id (continues to parse genTime via pkijs, NOT regex); add export class AllTsasFailed extends Error with .chain: TsaProviderId[]; add export async function requestTimestampWithFallback(sha256Hex, opts?:{ providers? }) that iterates providers in order, attempts requestTimestampFromProvider with that provider's timeoutMs (AbortController), then invokes verifyTsr against the provider's caCertPath before accepting (D-10 + pre-finalization verify); on any failure (network, timeout, non-2xx, parse, verify) push provider.id into chain and continue; if all fail, throw AllTsasFailed(chain); on success return {tsr, tsq, provider, attestedAt, fallbackChain: chain.concat(provider.id)}. Create src/lib/verifyTsr.ts exporting async function verifyTsr({tsr, dataPath, caCertPath}) that writes tsr to a tmp file and invokes execFile('openssl', ['ts','-verify','-in',tsrPath,'-data',dataPath,'-CAfile',caCertPath]) — throws on non-zero exit. Update src/lib/metadata.ts buildMetadata to accept fallback_chain and tsa_provider separately. Update src/routes/upload.ts: replace the single-provider call with requestTimestampWithFallback; on AllTsasFailed return c.json({error:'all_tsas_failed', chain: err.chain}, 502) and ensure no temp dir leak (try/finally also unlinks the per-request streamed upload temp file); on success pass fallback_chain + caCertPath (provider's caCertPath) into writeBundle so the correct PEM is copied as tsa-cacert.pem (D-10). The pre-finalization verify is owned by requestTimestampWithFallback, so by the time the route reaches writeBundle the TSR is already verified — no second call needed in the route. Implement the unit and E2E tests as described in <behavior>. The fallback E2E test uses env vars TSA_DFN_ENDPOINT and TSA_FREETSA_ENDPOINT to point at `http://127.0.0.1:1` (guaranteed-closed port) for the failure cases. The timeout-path unit test stands up a local HTTP server that delays its response via setTimeout(15_000) and asserts the provider is marked failed with a timeout (not ECONNREFUSED) signature in the chain reasoning. The DigiCert success e2e test (CONCERN-6) detects DigiCert reachability via a HEAD request in `beforeAll` and uses `test.skipIf(!digicertReachable)` only on CI; on the dev host it runs unconditionally. Run `npm test`.</action>
  <verify>
    <automated>npm test -- --run tests/unit/tsa.fallback.test.ts tests/e2e/upload.fallback.test.ts tests/e2e/upload.digicert-success.test.ts tests/e2e/upload.happy-path.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four test files pass (Plan 01-01 happy-path test still green) on the developer host
    - `grep -n "AllTsasFailed" src/lib/tsa.ts` returns a class export line
    - `grep -n "fallback_chain" src/lib/metadata.ts src/routes/upload.ts` returns at least one match per file
    - `grep -n "\-no_nonce" src/lib/tsa.ts src/lib/tsaProviders.ts` returns no matches (CONCERN-2)
    - With `TSA_DFN_ENDPOINT=http://127.0.0.1:1 npm run dev`, `curl -F file=@README.md http://localhost:3000/api/upload` returns 201 and the produced bundle's metadata.json has `tsa_provider === "freetsa"` and `tsa_fallback_chain === ["dfn","freetsa"]`
    - With `TSA_DFN_ENDPOINT=http://127.0.0.1:1 TSA_FREETSA_ENDPOINT=http://127.0.0.1:1 npm run dev`, the same curl returns 201 and metadata.tsa_provider === "digicert" with tsa_fallback_chain === ["dfn","freetsa","digicert"] (CONCERN-6, real DigiCert path proven)
    - With all three TSA_*_ENDPOINT vars set to `http://127.0.0.1:1`, the same curl returns HTTP 502 and the response body contains `"all_tsas_failed"` and DATA_DIR entry count is unchanged before and after (`ls DATA_DIR | wc -l` equals pre-call value)
    - In every successful bundle, `sha256sum <bundle>/tsa-cacert.pem` matches `sha256sum assets/tsa-certs/<tsa_provider>.pem`
    - `openssl ts -verify` against any of the dfn / freetsa / digicert bundles exits 0 using the bundled tsa-cacert.pem (BLOCKER-1: all three chains real and complete)
    - `assets/tsa-certs/README.md` documents the SHA-256 fingerprint of every cert in freetsa.pem and digicert.pem, AND records the live-TSA verification proof for each provider (BLOCKER-1)
    - The timeout test uses a sloth-endpoint mock (not just ECONNREFUSED) and asserts the timeout path explicitly
  </acceptance_criteria>
  <done>Full DFN→FreeTSA→DigiCert chain with per-candidate verify is live; D-05 invariant (zero disk artifacts on total failure) holds; D-10 CA-cert-matches-provider holds; the DigiCert leg is proven against the real endpoint (CONCERN-6); the AbortController timeout path is exercised by a real slow-response mock (nitpick). SEC-03 fully satisfied; CORE-02 fully satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify.sh in every bundle (CORE-04)</name>
  <files>assets/verify-template.sh, src/lib/bundle.ts, tests/e2e/verify-script.test.ts</files>
  <read_first>
    - src/lib/bundle.ts (current writeBundle implementation; note original.sha256 is ALREADY in `<hex>  original.<ext>\n` sha256sum -c format from Plan 01-01 per BLOCKER-3)
    - .planning/phases/01-core-archive-engine/01-CONTEXT.md (D-07 chmod 444 except verify.sh which needs +x; D-09 full chain semantics; D-10 CA cert match)
    - .planning/REQUIREMENTS.md (CORE-03 explicit file list, CORE-04)
    - https://docs.openssl.org/3.2/man1/openssl-ts/ (`-verify` flags)
  </read_first>
  <behavior>
    - Test verify-script.test.ts: after a successful upload, the bundle contains a verify.sh file at mode 0555 (read+execute, no write)
    - Test: invoking `bash <bundle>/verify.sh` from a CWD inside the bundle exits 0 and stdout contains "VERIFICATION SUCCESS"
    - Test: invoking `sha256sum -c <bundle>/original.sha256` from inside a writable copy of the bundle exits 0 (proves the day-1 sha256sum -c format from Plan 01-01 is consumed correctly)
    - Test: tampering with original.txt (echo "x" >> via a writable copy of the bundle) causes verify.sh to exit non-zero and stdout to contain "SHA256 MISMATCH"
    - Test: replacing original.tsr with random bytes causes verify.sh to exit non-zero and stdout to contain "TIMESTAMP VERIFICATION FAILED"
    - Test: verify.sh references files by relative path only (no absolute paths embedded) — `grep -E "/(Users|home|tmp)/" verify.sh` finds no matches
  </behavior>
  <action>Create assets/verify-template.sh as a POSIX sh script (shebang `#!/bin/sh`, `set -eu`) that: 1) determines its own directory via `DIR="$(cd "$(dirname "$0")" && pwd)"`, 2) finds the original file by globbing `original.*` (excluding .sha256, .tsq, .tsr) — assert exactly one match into ORIGINAL, 3) runs `sha256sum -c original.sha256` (use `shasum -a 256 -c` on macOS — handle both via `command -v sha256sum || command -v shasum`); on failure echo "SHA256 MISMATCH" and exit 2, 4) runs `openssl ts -verify -in original.tsr -data "$ORIGINAL" -CAfile tsa-cacert.pem`; on failure echo "TIMESTAMP VERIFICATION FAILED" and exit 3, 5) on full success echo "VERIFICATION SUCCESS: $ORIGINAL hashes match and timestamp is valid" and exit 0. **Do NOT change the original.sha256 format** — Plan 01-01 already writes it in `<hex>  original.<ext>\n` (two spaces, sha256sum -c compatible) per BLOCKER-3; this task only consumes it. Update src/lib/bundle.ts to also `fs.copyFile('assets/verify-template.sh', path.join(tempDir, 'verify.sh'))` before the rename, and after the rename `fs.chmod(path.join(finalDir,'verify.sh'), 0o555)` while other files remain `0o444`. Implement tests/e2e/verify-script.test.ts that uploads via the existing happy-path flow, copies the resulting bundle to a writable tmp location (since bundle is 444), then runs the four scenarios from <behavior>. Run `npm test`.</action>
  <verify>
    <automated>npm test -- --run tests/e2e/verify-script.test.ts tests/e2e/upload.happy-path.test.ts tests/e2e/upload.fallback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All E2E tests pass (incl. the unchanged Plan 01-01 happy-path test, which already asserts `sha256sum -c` on original.sha256 — proving Plan 02 did not regress the format)
    - For any produced bundle: `stat -c %a <bundle>/verify.sh 2>/dev/null || stat -f %Lp <bundle>/verify.sh` returns `555`
    - For any produced bundle: `stat -c %a <bundle>/metadata.json 2>/dev/null || stat -f %Lp <bundle>/metadata.json` returns `444` (and same for original.*, tsa-cacert.pem)
    - `bash <bundle>/verify.sh` exits 0 and stdout contains the literal string `VERIFICATION SUCCESS`
    - `(cd <bundle> && sha256sum -c original.sha256)` exits 0 (consumes the day-1 format)
    - Bundle contains exactly 7 files: `original.<ext>`, `original.sha256`, `original.tsq`, `original.tsr`, `tsa-cacert.pem`, `metadata.json`, `verify.sh` (`ls -1 <bundle> | wc -l` equals 7)
    - `grep -vE '^[[:space:]]*#' assets/verify-template.sh | grep -cE '/(Users|home|tmp)/'` returns 0 (no absolute paths in non-comment lines) — uses comment-stripped grep to avoid false positives from header prose
    - Tamper test on a writable copy: appending a byte to `original.<ext>` causes `bash verify.sh` to exit non-zero with `SHA256 MISMATCH` in stdout
    - Tamper test: overwriting `original.tsr` with `head -c 100 /dev/urandom` causes exit non-zero with `TIMESTAMP VERIFICATION FAILED` in stdout
  </acceptance_criteria>
  <done>CORE-03 fully satisfied (all 7 artifacts present), CORE-04 fully satisfied (verify.sh performs full chain check), D-07 immutability semantics applied per-file (444 for data, 555 for verify.sh). Bundles are now self-contained: a user can hand a bundle to a third party who, with only openssl and sh installed, can verify integrity offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node service → second/third TSA | Same TLS/cert-validation boundary as DFN; FreeTSA over HTTPS, DigiCert over HTTP (no TLS) — TSR is signed end-to-end so TLS is defense-in-depth only |
| verify.sh → bundle filesystem | verify.sh runs on third-party hosts with unknown openssl versions; must avoid bashisms and assume `/bin/sh` (dash on Debian) |
| Service → env var TSA endpoints | TSA_*_ENDPOINT overrides could be abused by attacker with env access to redirect TSA traffic to attacker-controlled server, then forge signature — mitigated because pre-finalization verify checks signature against the COMMITTED CA cert (not a fetched one) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TSR replaced in transit or by hostile TSA endpoint | mitigate | `verifyTsr` runs `openssl ts -verify` with the **committed** CA cert chain (assets/tsa-certs/<provider>.pem) BEFORE the bundle is written. A forged TSR fails signature verification and the provider is skipped. |
| T-02-02 | Tampering | CA cert swapped at runtime via env var or filesystem | accept (Phase 1) | CA certs are committed into the image (Plan 03 bakes them in). At runtime the path is relative to the working dir; no env override exists. Phase 2+ may add a `--trusted-cert-bundle` flag. |
| T-02-03 | DoS | All three TSAs slow → request hangs | mitigate | Per-provider AbortController timeout (default 10s, configurable via TSA_TIMEOUT_MS); worst case 3 × timeout for the full chain. The timeout path is exercised by a slow-response mock test, not only by ECONNREFUSED. |
| T-02-04 | Repudiation | metadata.json claims tsa_provider that does not match the actual signer | mitigate | tsa_provider is set from the provider whose verifyTsr **succeeded**; CA cert copied into bundle is that same provider's cert; verify.sh re-checks both at any later time. |
| T-02-05 | Elevation of privilege | verify.sh interpreted by a maliciously crafted /bin/sh | accept | Out of scope — user runs verify.sh on their own machine; they trust their own shell. |
| T-02-06 | Information disclosure | verify.sh embeds absolute paths from the build host | mitigate | verify.sh uses only `$(dirname "$0")`-relative paths; grep gate (comment-stripped) in acceptance_criteria asserts no `/Users/` `/home/` `/tmp/` substrings in non-comment lines. |
| T-02-07 | DoS | Repeated upload attempts during TSA outage flood logs / disk | accept (Phase 1) | Phase 1 has no auth so attacker has unbounded calls anyway; rate-limit + auth land in Phase 2. D-05 ensures failed attempts cost nothing on disk. |
| T-02-08 | Tampering | Committed CA chain incomplete → false-negative verify failures OR false-positive verify successes if a partial chain accidentally validates | mitigate | The cert-chain build procedure (Plan 01-01 + Plan 02 BLOCKER-1) requires `openssl ts -verify` against a real TSR to exit 0 BEFORE commit; fingerprints recorded in README.md make tampering auditable. |
</threat_model>

<verification>
- `npm test` runs the full vitest suite (unit + 4 e2e files) with 0 failures on the developer host
- Manual: produce three bundles by toggling TSA_*_ENDPOINT env vars and confirm tsa_provider in metadata.json cycles dfn / freetsa / digicert and `openssl ts -verify` exits 0 for all three
- Manual: copy a bundle to /tmp/bundle-copy, `chmod -R u+w /tmp/bundle-copy`, run the tamper scenarios from Task 2 acceptance
- Manual: with all three TSA endpoints set to `http://127.0.0.1:1`, confirm `DATA_DIR` contents are identical before and after a curl POST
- Manual: confirm cert fingerprints recorded in assets/tsa-certs/README.md match `openssl x509 -in <cert> -noout -fingerprint -sha256` for each cert in each chain
</verification>

<success_criteria>
- Roadmap SC-2 satisfied: TSR is verified by OpenSSL immediately after receipt; metadata.json records tsa_provider, tsa_status, attested timestamp
- Roadmap SC-3 satisfied: DFN unreachable → automatic FreeTSA retry → DigiCert retry → fallback provider recorded in metadata.json; the DigiCert leg is proven against the real endpoint
- Roadmap SC-4 satisfied: verify.sh from bundle directory exits 0 and prints success message; sha256sum -c also succeeds standalone
- D-05 invariant holds under failure injection
- All seven CORE-03 artifacts present in every successful bundle
- All three committed CA chains (dfn, freetsa, digicert) are real (root + intermediates), proven via openssl ts -verify exit 0 before commit, with fingerprints recorded
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-archive-engine/01-02-SUMMARY.md` recording: actual FreeTSA + DigiCert endpoint response shapes, observed cert-chain composition for each TSA, any deviations from D-12 (especially fallback_chain semantics), SHA-256 fingerprint of every cert in every committed CA PEM, the recorded openssl ts -verify exit-0 proof for each chain, and any sh portability gotchas hit while writing verify.sh.
</output>
