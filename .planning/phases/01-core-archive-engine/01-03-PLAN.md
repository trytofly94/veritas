---
phase: 01-core-archive-engine
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
  - README.md
  - scripts/smoke-container.sh
  - tests/e2e/container-smoke.test.ts
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, SEC-03, META-01, META-02, META-03]
tags: [docker, unraid, deployment, smoke-test]
must_haves:
  truths:
    - "docker compose up builds and runs the service from node:22-bookworm-slim with openssl present and working"
    - "All bundle data lands on the host bind-mounted volume (./data/) — nothing inside the container"
    - "A curl POST against the running container produces a bundle on the host volume"
    - "verify.sh inside the produced bundle exits 0 when run on the host (not inside the container) using the host's openssl"
    - "The service starts unchanged on the Unraid server (192.168.178.30) via docker compose up using the same compose file"
    - "The container's HEALTHCHECK uses Node 22 built-in fetch (no apt-installed curl) so the image stays minimal"
    - "The container has an explicit container_name=auto-archive so 'docker compose exec' commands are stable across compose project naming"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: deps -> build -> runtime on node:22-bookworm-slim with openssl + a non-root user + Node-based HEALTHCHECK"
    - path: "docker-compose.yml"
      provides: "Service definition with container_name=auto-archive, bind mount ./data:/data, port 3000:3000, env DATA_DIR=/data"
    - path: "scripts/smoke-container.sh"
      provides: "Automated container smoke: build, up, curl POST, assert bundle on host, verify.sh, down"
    - path: "tests/e2e/container-smoke.test.ts"
      provides: "Vitest wrapper that runs scripts/smoke-container.sh and asserts exit 0 + bundle artifacts"
    - path: "README.md"
      provides: "Local-run + Unraid-deploy + curl example instructions"
  key_links:
    - from: "docker-compose.yml"
      to: "./data on host -> /data inside container"
      via: "bind mount; DATA_DIR=/data env"
      pattern: "- \\./data:/data"
    - from: "Dockerfile runtime stage"
      to: "openssl binary"
      via: "node:22-bookworm-slim ships openssl; verified at build time with RUN openssl version"
      pattern: "openssl version"
    - from: "Container process"
      to: "Outbound HTTPS to DFN / FreeTSA / DigiCert"
      via: "Docker default bridge network (per STATE.md blocker — must be confirmed)"
      pattern: "fetch.*https://"
---

<objective>
Wave 3 of Phase 1. Containerize the service from Plan 01-02 so it runs unchanged on Unraid. This plan addresses roadmap Success Criterion 5 (the container starts from `docker compose up` with all data on bind-mounted volumes, nothing stored inside the container) and closes the Phase 1 deliverable. Includes a Unraid checkpoint where the developer deploys the image to 192.168.178.30 and confirms a real submission end-to-end on real hardware — addressing STATE.md blocker "Confirm DFN-TSA HTTP endpoint reachability from inside Docker on Unraid before committing as primary".

Purpose: Move the working service from "runs on my laptop" to "runs in production on Unraid" without changing the application code. After this plan, the phase goal is achieved: a curl POST against the Unraid-hosted container produces a tamper-proof bundle on the bind-mounted volume.

Output: A buildable Dockerfile + docker-compose.yml; an automated container smoke test; documented deploy steps; a human-verified production smoke test on Unraid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-archive-engine/01-CONTEXT.md
@.planning/phases/01-core-archive-engine/01-SKELETON.md
@.planning/phases/01-core-archive-engine/01-01-SUMMARY.md
@.planning/phases/01-core-archive-engine/01-02-SUMMARY.md
@.planning/research/PITFALLS.md
@package.json
@src/index.ts
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + docker-compose.yml + container smoke test</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore, scripts/smoke-container.sh, tests/e2e/container-smoke.test.ts, README.md</files>
  <read_first>
    - .planning/phases/01-core-archive-engine/01-CONTEXT.md (D-02 bookworm-slim, not Alpine; D-07 chmod 444; D-11 no SQLite)
    - .planning/phases/01-core-archive-engine/01-SKELETON.md (Stack Touched + Deployment row)
    - .planning/research/PITFALLS.md (Alpine vs Debian Docker base — native modules; outbound HTTPS through Docker default bridge)
    - package.json (scripts: build, start)
    - src/index.ts (PORT env default; DATA_DIR env)
    - CLAUDE.md ("Not: Node.js Alpine Docker image"; Node 22 has fetch built in — used for HEALTHCHECK)
  </read_first>
  <action>Create a multi-stage Dockerfile based on `node:22-bookworm-slim` (D-02). Stage `deps` runs `npm ci --omit=dev` against a copied package.json + package-lock.json into /app/node_modules-prod. Stage `build` runs `npm ci` then `npm run build` producing dist/. Stage `runtime` starts from `node:22-bookworm-slim`, runs `RUN apt-get update && apt-get install -y --no-install-recommends openssl ca-certificates && rm -rf /var/lib/apt/lists/*` then `RUN openssl version` (build-time assertion that openssl is present). **Do NOT install curl** (CONCERN-5) — the HEALTHCHECK will use Node 22's built-in fetch. Create a non-root user `app` (uid 10001), copy node_modules-prod from `deps` and dist/ + assets/ from `build` into /app, set WORKDIR /app, USER app, ENV NODE_ENV=production DATA_DIR=/data PORT=3000, declare VOLUME /data, EXPOSE 3000, CMD ["node","dist/index.js"]. **HEALTHCHECK uses Node fetch (CONCERN-5):** `HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 CMD node -e "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"`. NOTE: this requires a `GET /health` route in src/server.ts returning 200 with body `{"ok":true}` — add this route as part of this task (small enough to fit; do not split into a separate task). Create docker-compose.yml with one service `auto-archive`: **`container_name: auto-archive`** (CONCERN-4 — pins the container name regardless of compose project naming so `docker compose exec` / `docker exec` invocations are deterministic), build context `.`, image `auto-archive:phase1`, ports `["3000:3000"]`, volumes `["./data:/data"]`, restart `unless-stopped`, environment block forwarding optional TSA_*_ENDPOINT overrides and TSA_TIMEOUT_MS. Update .dockerignore to ensure /data and tests/ are excluded so test bundles never bake into the image. Create scripts/smoke-container.sh (POSIX sh, set -eu): clean up any prior `auto-archive` container, ensure ./data exists and is empty, `docker compose build`, `docker compose up -d`, wait for /health to respond 200 (poll up to 30s using `node -e "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1))"` — keep parity with HEALTHCHECK; no curl), POST tests/fixtures/hello.txt via curl on the host (curl on host is fine — only the container avoids curl), capture the JSON, assert HTTP 201, assert the returned bundle_path under /data maps to a directory under ./data on host with the 7 CORE-03 files, run `bash ./data/<id>/verify.sh` and assert exit 0 + "VERIFICATION SUCCESS" stdout, **assert no /app/data exists inside the container via the clean compose-exec form (CONCERN-4):** `docker compose exec -T auto-archive sh -c '[ ! -e /app/data ]'` — exits 0 iff the path is absent (no grep, no name ambiguity), `docker compose down`. Wrap the script in tests/e2e/container-smoke.test.ts via vitest's `execSync('bash scripts/smoke-container.sh')` so CI can gate on it. Update README.md with: prerequisites (Docker, openssl on host for verification), `docker compose up -d` quickstart, the curl example, the verify.sh example, and a "Deploy to Unraid" section that references the next task's manual checkpoint. Add an explicit note that the bundle file ownership inside ./data on the host is uid 10001 — document that the user can `chown` if Unraid requires a specific uid (Unraid default share uid is 99/users). Also include the existing no-auth warning from Plan 01-01 prominently in README.md ("⚠ Phase 1 has no auth — DO NOT expose port 3000 to the public internet").</action>
  <verify>
    <automated>bash scripts/smoke-container.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose build` exits 0; image `auto-archive:phase1` is created
    - `docker run --rm auto-archive:phase1 openssl version` prints an OpenSSL version line and exits 0
    - `docker inspect auto-archive:phase1 --format '{{.Config.User}}'` returns `app` (non-root)
    - `docker run --rm auto-archive:phase1 sh -c "command -v curl"` returns a non-zero exit (curl NOT installed — CONCERN-5)
    - `docker inspect auto-archive:phase1 --format '{{json .Config.Healthcheck.Test}}'` contains the literal substring `"fetch('http://127.0.0.1:3000/health')"` (CONCERN-5)
    - `bash scripts/smoke-container.sh` exits 0
    - `docker ps --filter name=^auto-archive$ --format '{{.Names}}'` returns exactly `auto-archive` after `docker compose up -d` (CONCERN-4: container_name pinned)
    - After the smoke script runs, `ls -1 ./data` shows exactly one ULID directory created during the run
    - That directory contains exactly 7 files (per Plan 02 CORE-03 final state)
    - `bash ./data/<id>/verify.sh` exits 0 from the host shell (host openssl, not container openssl) and stdout contains `VERIFICATION SUCCESS`
    - **Container-bundle-isolation check (CONCERN-4):** `docker compose exec -T auto-archive sh -c '[ ! -e /app/data ]'` exits 0 (clean boolean — no grep, no name dependency)
    - `docker compose down` leaves ./data populated; bundles persist across container restarts
    - `npm test -- --run tests/e2e/container-smoke.test.ts` exits 0
    - In scripts/smoke-container.sh, ALL `stat` invocations against bundle file modes use the dual form `stat -c %a <path> 2>/dev/null || stat -f %Lp <path>` (Linux + macOS portability — nitpick)
  </acceptance_criteria>
  <done>Local Docker run works end-to-end with bind-mounted data; automated smoke test passes; CORE-03 bundle ends up on host, not inside container; HEALTHCHECK uses Node fetch (no curl bloat); container name is pinned for stable exec commands. Ready for Unraid deploy in next task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Unraid production smoke test (resolves STATE.md blocker)</name>
  <files>README.md, .planning/phases/01-core-archive-engine/01-UNRAID-VERIFY.md</files>
  <read_first>
    - README.md "Deploy to Unraid" section authored in Task 1
    - .planning/STATE.md (Blockers/Concerns: "Confirm DFN-TSA HTTP endpoint reachability from inside Docker on Unraid")
    - Project user's global skill `unraid-server` (~/.claude/skills/unraid-server) for the standard Unraid deploy procedure
  </read_first>
  <what-built>
    A Dockerized auto-archive service that, on the developer's laptop, accepts a multipart POST, hashes the file, obtains a real RFC 3161 timestamp from DFN (with FreeTSA + DigiCert fallback), verifies it with OpenSSL, and writes a complete 7-file bundle to a bind-mounted volume — all without storing anything inside the container. This task verifies the same image runs on the actual Unraid server at 192.168.178.30 and reaches DFN-TSA through the Unraid Docker bridge.
  </what-built>
  <how-to-verify>
    1. Copy the repository (or just docker-compose.yml + Dockerfile + assets/ + package.json + src/) to Unraid: `rsync -avz ./ root@192.168.178.30:/mnt/user/appdata/auto-archive/`
    2. SSH to Unraid: `ssh root@192.168.178.30`
    3. `cd /mnt/user/appdata/auto-archive && mkdir -p data && docker compose build && docker compose up -d`
    4. From your local machine: `curl -F file=@tests/fixtures/hello.txt -F label=unraid-smoke http://192.168.178.30:3000/api/upload` — assert HTTP 201 + JSON with `id` and `bundle_path`
    5. SSH to Unraid: `ls -la /mnt/user/appdata/auto-archive/data/<id>/` — assert 7 files present, all owned by uid 10001 (or whichever user docker runs containers as on this Unraid install)
    6. SSH to Unraid: `cat /mnt/user/appdata/auto-archive/data/<id>/metadata.json | jq .tsa_provider` — record which TSA actually responded (most likely `"dfn"` if DFN is reachable; if it shows `"freetsa"` or `"digicert"`, DFN is NOT reachable from the Unraid Docker bridge — capture this in the verification report)
    7. SSH to Unraid: `bash /mnt/user/appdata/auto-archive/data/<id>/verify.sh` — assert exit 0 + "VERIFICATION SUCCESS" on real Unraid hardware
    8. SSH to Unraid: `docker compose exec -T auto-archive sh -c '[ ! -e /app/data ]'` — assert exit 0 (CONCERN-4 isolation check on real Unraid)
    9. Stop and remove: `docker compose down` — confirm `./data` survives
    10. Write a brief verification report at `.planning/phases/01-core-archive-engine/01-UNRAID-VERIFY.md` recording: image build time, actual tsa_provider observed (resolves STATE.md blocker), latency of TSA round-trip from Unraid, host uid of bundle files, any deviations
    11. If DFN was NOT reachable: note this in STATE.md as a follow-up; the system still works because of the fallback chain — no code change needed in Phase 1
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/01-core-archive-engine/01-UNRAID-VERIFY.md` exists and documents observed tsa_provider, latency, host uid, and any deviations
    - Step 4 returns HTTP 201
    - Step 5 shows exactly 7 files in the bundle directory on the Unraid host filesystem
    - Step 7 prints "VERIFICATION SUCCESS" and exits 0 on the Unraid host (this is the literal observable evidence of the phase goal)
    - Step 8 exits 0 (no /app/data inside the container)
    - Step 9 confirms data persists after container removal
    - STATE.md Blockers/Concerns updated: the "Confirm DFN-TSA HTTP endpoint reachability from inside Docker on Unraid" entry is either struck through (if DFN worked) or annotated with the observed fallback provider (if DFN was unreachable)
  </acceptance_criteria>
  <resume-signal>Type `approved` after verification report is committed and STATE.md updated. If any step fails, describe the failure and the team will branch to a gap-closure plan instead of resuming.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host kernel → container | Container should drop capabilities, run non-root, read-only rootfs optional |
| Container → host bind mount | Container writes to `/data`; mapped to `./data` on host; uid/gid permissions matter |
| External (LAN) → container port 3000 | Phase 1 has no auth, so port 3000 must NOT be published outside the LAN until Phase 2 layers API-key middleware |
| Image build → npm registry | Supply chain risk; mitigated by `npm ci` + committed lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of privilege | Container runs as root | mitigate | Dockerfile creates user `app` (uid 10001), drops to USER app before CMD. Verified via `docker inspect --format '{{.Config.User}}'` in acceptance criteria. |
| T-03-02 | Information disclosure | Image accidentally bakes in test data or .env | mitigate | .dockerignore excludes tests/, data/, .env*, node_modules. `docker history auto-archive:phase1` reviewed manually during Task 1. |
| T-03-03 | Tampering | Bundle files on host owned by container uid → host user cannot read | mitigate | Documented in README; user can `chown -R 99:100 ./data` (Unraid nobody:users) if needed. Phase 1 leaves uid 10001; Phase 2 can revisit if it causes friction. |
| T-03-04 | DoS | Unbounded /data volume growth | accept (Phase 1) | No quota in Phase 1; user owns disk capacity. Documented in README. Phase 2+ may add retention policies. |
| T-03-05 | Information disclosure | Port 3000 reachable from LAN without auth | mitigate | docker-compose.yml binds `3000:3000` (all interfaces) but README explicitly warns: "Phase 1 has no auth; DO NOT expose port 3000 to the internet. Phase 2 introduces API-key middleware before publishing via Cloudflare Tunnel." STATE.md blocker captures this; Plan 01-01 also emits a startup `console.warn`. |
| T-03-06 | Tampering | Outbound TSA traffic blocked by Unraid network policy → silent fallback to DigiCert without operator noticing | mitigate | The Unraid checkpoint (Task 2) explicitly reads tsa_provider from the first real submission and records it. If DFN isn't reachable, the operator sees it immediately and decides whether to fix Unraid networking or accept FreeTSA primary. |
| T-03-07 | Supply chain | Compromised npm dependency | accept (Phase 1) | `npm ci` against committed lockfile is the baseline mitigation; Dependabot/Renovate considered v2. |
| T-03-08 | DoS | HEALTHCHECK depending on apt-installed curl creates image bloat + attack surface | mitigate | HEALTHCHECK uses Node 22 built-in fetch (CONCERN-5); no curl in the runtime image. |
</threat_model>

<verification>
- `bash scripts/smoke-container.sh` passes on developer machine (Task 1)
- `npm test -- --run tests/e2e/container-smoke.test.ts` passes on developer machine
- All Plan 01 + Plan 02 tests still pass: `npm test`
- Human-verified Unraid smoke succeeds (Task 2)
- `.planning/phases/01-core-archive-engine/01-UNRAID-VERIFY.md` committed
- STATE.md DFN-reachability blocker resolved
</verification>

<success_criteria>
- Roadmap SC-5 satisfied: container starts from `docker compose up` with all data on bind-mounted volumes — confirmed both locally AND on Unraid
- STATE.md blocker "Confirm DFN-TSA HTTP endpoint reachability from inside Docker on Unraid" resolved with empirical evidence
- The phase goal sentence is now literally true: "A running Docker container on Unraid can accept a file, hash it, obtain an RFC 3161 timestamp, and write a complete tamper-proof archive bundle to a bind-mounted volume."
- The container image stays minimal (no curl) and HEALTHCHECK uses Node fetch
- The container_name is pinned so isolation assertions are deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-archive-engine/01-03-SUMMARY.md` recording: final image size, image build time, container memory footprint at idle, observed Unraid uid of bundle files, observed tsa_provider on Unraid (resolves blocker), any Dockerfile tweaks needed for bookworm-slim, link to `.planning/phases/01-core-archive-engine/01-UNRAID-VERIFY.md`.
</output>
